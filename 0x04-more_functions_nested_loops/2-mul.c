#include "holberton.h"

/**
 * mul - multiplication of to numbers
 * @a: - parameter
 * @b: - parameter
 * Return: 1 for digit, 0 for anything else
 */

int mul(int a, int b)
{
	int pro;

	pro = a * b;
	return (pro);
}
